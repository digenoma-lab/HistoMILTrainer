name: histomil
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - git
  - pip:
    - git+https://github.com/mahmoodlab/MIL-Lab.git #Replace it with original when the PR is merged
    - git+https://github.com/oval-group/smooth-topk.git # Required for CLAM
    - seaborn
    - matplotlib
    - pytest